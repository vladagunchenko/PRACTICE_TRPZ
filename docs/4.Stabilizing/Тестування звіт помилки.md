 # ТЕСТУВАННЯ І ЗВІТ ПРО ПОМИЛКИ

Етап: *Тестування (Stabilizing)*

Проект: *NotePro*

Команда: *Programists*

Виконали:
>*Савеля Максим*

>*Зорій Ярослав*

## **1. ВСТУП**

Цей документ являє собою огляд результатів тестування веб-застосунку NotePro на різних етапах розробки. Він використовується повторно протягом фаз розробки, тестування та стабілізації для відстеження якості продукту.

Тестування є критично важливим етапом, оскільки дозволяє виявити помилки, оцінити стабільність роботи системи та впливає на строки завершення проєкту.

## **2. МЕТОДИ ТА ІНСТРУМЕНТИ ТЕСТУВАННЯ**
У процесі тестування більшість запланованих методів була реалізована, проте деякі виконані частково.

Відмінності від плану:
- Повністю виконано: функціональне, UI, модульне тестування;
- Частково виконано: автоматизоване тестування (через обмеження часу);
- Тестування продуктивності виконано лише базово (без глибокого навантаження);
- Регресійне тестування проводилось вручну, без повної автоматизації.

Причина: обмежені ресурси та навчальний формат проєкту.

### **2.1. Огляд методів тестування**

- Модульне тестування - перевірка окремих компонентів (авторизація, робота з БД);
- Функціональне тестування - перевірка основних сценаріїв (створення, редагування, видалення завдань);
- UI тестування - перевірка відображення інтерфейсу на різних пристроях;
- Usability тестування - оцінка зручності користування;
- Compatibility тестування - перевірка роботи у різних браузерах;
- Performance тестування - оцінка швидкодії;
- Регресійне тестування - перевірка після внесення змін.

### **2.2. Огляд засобів тестування**

- Google Lighthouse - аналіз продуктивності та швидкодії;
- Testpad - ручне тестування сценаріїв;
- testRigor - базове автоматизоване тестування;
- Testsigma - перевірка регресійних сценаріїв;
- BrowserStack - тестування сумісності браузерів;
- Інструменти браузера (DevTools) - перевірка помилок та логів.

## **3. ЗВІТ ПРО ТЕСТИ**

### **3.1. Результати тестів - відмінності від минулого звіту**

Виконано такі тести:
- Авторизація користувача - пройдено успішно;
- Створення завдання - працює, але є дрібні баги;
- Редагування - працює стабільно;
- Видалення - працює, але є затримка оновлення списку;
- Пошук і фільтрація - працює частково некоректно;
- UI - незначні проблеми на мобільних.

### **3.2. Відхилення у виконанні плану тестування**

- Автоматизоване тестування виконано не повністю;
- Не проводилось повноцінне навантажувальне тестування;
- Частина тестів виконана пізніше через затримки розробки.

Причини:
- обмежений час;
- зміни у функціоналі під час розробки.

### **3.3. Решта тестів та виправлення в плані тестування**

Ще потрібно виконати:
- повне регресійне тестування;
- розширене тестування продуктивності;
- тестування під навантаженням.

Зміни в плані:
- додати тестування API;
- більше уваги приділити мобільній версії.

## **4. ЗОНИ ТЕСТУВАННЯ**

Реалізація веб-застосунку NotePro була поділена на окремі функціональні зони тестування, що дозволило оцінити якість роботи основних підсистем, виявити слабкі місця та сформувати рекомендації щодо покращення стабільності й зручності використання системи.

### **4.1. Зона 1 – Авторизація та доступ користувача**

**ЦІЛІ ТЕСТУВАННЯ**

Метою тестування цієї зони була перевірка коректності роботи механізму авторизації користувача, зокрема входу до системи, перевірки правильності введених даних, обмеження доступу для неавторизованих користувачів та стабільності переходу між сторінками після входу.

**КРИТЕРІЇ ОЦІНЮВАННЯ**

Тестування вважається успішним, якщо:
- користувач може увійти до системи з коректними обліковими даними;
- при введенні неправильного логіна або пароля система відображає повідомлення про помилку;
- неавторизований користувач не отримує доступ до захищених сторінок;
- після успішного входу користувач перенаправляється до основного робочого інтерфейсу;
- сесія користувача зберігається коректно протягом роботи.

**РЕЗУЛЬТАТИ**

У ході тестування встановлено, що авторизація в системі працює загалом стабільно. Вхід із правильними даними здійснюється коректно, а при введенні некоректних облікових даних користувач отримує відповідне повідомлення. Доступ до основних функцій NotePro без попередньої авторизації обмежений. Критичних помилок у цій зоні не виявлено, тому тестування можна вважати успішним.

**РЕКОМЕНДАЦІЇ**

Рекомендується:
- покращити інформативність повідомлень про помилки входу;
- передбачити додаткову перевірку на порожні поля перед відправленням форми;
- реалізувати механізм відновлення пароля або спрощене керування сесією користувача;
- додатково перевірити безпечність обробки даних авторизації.

### **4.2. Зона 2 – Керування завданнями**

**ЦІЛІ ТЕСТУВАННЯ**

Метою тестування цієї зони була перевірка коректності основного функціоналу застосунку, а саме створення, редагування, видалення, пошуку, фільтрації та відображення завдань у списку.

**КРИТЕРІЇ ОЦІНЮВАННЯ**

Тестування вважається успішним, якщо:
- нове завдання успішно створюється та відображається у списку;
- редагування завдання зберігає внесені зміни без втрати даних;
- після видалення завдання воно зникає зі списку без необхідності повторного завантаження сторінки;
- пошук і фільтрація коректно відображають потрібні записи;
- інтерфейс списку завдань правильно працює як на комп’ютері, так і на мобільних пристроях.

**РЕЗУЛЬТАТИ**

За результатами тестування встановлено, що функції створення та редагування завдань працюють коректно і стабільно. Видалення завдань також виконується успішно, однак в окремих випадках спостерігається незначна затримка в оновленні списку після виконання дії. Функції пошуку й фільтрації працюють частково некоректно, зокрема в окремих сценаріях результати відображаються не повністю або з помилками. Крім того, на мобільних пристроях виявлено незначні проблеми з відображенням окремих елементів інтерфейсу. У цілому зона є працездатною, але потребує додаткового доопрацювання.

**РЕКОМЕНДАЦІЇ**

Рекомендується:
- усунути затримку оновлення списку після видалення завдання;
- доопрацювати логіку пошуку та фільтрації для коректної обробки всіх сценаріїв;
- покращити адаптивність інтерфейсу для мобільних пристроїв;
- провести повторне регресійне тестування після внесення змін;
- розширити перелік тестових сценаріїв для перевірки роботи із завданнями при різних комбінаціях даних.

## **5. ЗВІТ ПРО ПОМИЛКИ**

Звіт про помилки містить інформацію про те, **які помилки було знайдено** в ході тестування. Наявність такої інформації дозволяє зрозуміти **якість поточної збірки** і обсяг ресурсів, які потрібно витратити для його доведення до прийнятного стану.

### **5.1. Статус збірки**

Поточний статус збірки можна оцінити як **стабільний**. У ході тестування було перевірено основні функціональні можливості веб-застосунку **NotePro**, зокрема авторизацію користувача, створення, редагування, пошук, фільтрацію та видалення завдань, а також коректність відображення інтерфейсу. За результатами перевірки **критичних або значних помилок, що впливають на працездатність системи, виявлено не було**.

Основний функціонал застосунку працює коректно, інтерфейс відображається належним чином, а використання системи не викликає суттєвих труднощів. За результатами тестування команда перебуває на етапі **стабілізації продукту**, коли основна розробка вже завершена, а подальша робота полягає у підтримці стабільності, незначних покращеннях і підготовці продукту до завершення робіт.

### **5.2. Відомі помилки**

У ході проведеного тестування **критичних, значних або таких, що впливають на основний функціонал системи, помилок виявлено не було**.

Під час перевірки основних сценаріїв роботи веб-застосунку NotePro було встановлено, що авторизація, створення, редагування, пошук, фільтрація та видалення завдань виконуються коректно. Також не було виявлено суттєвих проблем із відображенням інтерфейсу чи виконанням основних дій користувача.

Можливі окремі незначні зауваження до оформлення або дрібних деталей інтерфейсу не мають критичного впливу на роботу системи та не перешкоджають її використанню.

### **5.3. Аналіз збіжності помилок**

У цьому розділі виконується аналіз *поточної кількості помилок, кількості знову виявлених помилок, кількості усунених помилок*.

У **точці конвергенції** *(bug convergence)* стає помітним суттєвий прогрес в усуненні помилок, тобто швидкість усунення помилок починає перевершувати швидкість їх виявлення.

![Рисунок 5.1 –Точка конвергенції.](../images/4.%20Stabilizing/convergence.png)

Оскільки кількість знайдених, але не усунених помилок може коливатися навіть після того, як вона почала спадати, конвергенція може розглядатися **швидше як тенденція**, ніж як фіксований момент у часі. Після проходження цієї віхи кількість активних помилок має продовжувати зменшуватися, аж до точки досягнення нуля. *Точка конвергенції дає проектній групі можливість зрозуміти, що процес тестування наближається до кінця.*

**Точка досягнення нуля** *(zero-bug bounce)* - це момент, коли вперше всі виявлені помилки виявляються усуненими. Надалі пікові значення кількості активних помилок мають ставати все меншими, аж до повного згасання в момент, коли рішення вже досить стабільне для випуску першої версії (реліз-кандидата).

![Рисунок 5.2 –Точка досягнення нуля.](../images/4.%20Stabilizing/bug-bounce.jpg)

У процесі тестування веб-застосунку **NotePro** кількість виявлених помилок була мінімальною. Під час проходження запланованих тестових сценаріїв не було зафіксовано критичних дефектів або значних збоїв, що потребували б суттєвого доопрацювання функціоналу. Це свідчить про достатній рівень стабільності продукту на поточному етапі.

**Точку конвергенції помилок** можна вважати досягнутою на етапі завершення основного проходження тестових сценаріїв, коли повторні перевірки не виявили нових суттєвих дефектів, а кількість активних зауважень залишалася відсутньою або незначною.

**Точку досягнення нуля** для поточного етапу можна вважати умовно досягнутою під час завершального тестування, оскільки після проходження основних сценаріїв не було виявлено помилок, що впливають на працездатність або стабільність системи.

## **6. ОНОВЛЕНИЙ ПЛАН ТЕСТУВАННЯ**

За результатами проведеного тестування суттєвих змін до плану тестування вносити не потрібно. Основні передбачені сценарії перевірки були виконані відповідно до підготовлених тестів і не виявили критичних або значних помилок у роботі веб-застосунку **NotePro**.

Під час тестування для фіксації та проходження тестових сценаріїв використовувався **Testpad**, що дозволило впорядкувати перевірку основного функціоналу та зафіксувати результати виконання тестів. Перевірено коректність роботи авторизації, створення, редагування, пошуку, фільтрації та видалення завдань, а також базову перевірку інтерфейсу користувача.

Надалі доцільно передбачити **повторне регресійне тестування** після внесення будь-яких змін до функціоналу або інтерфейсу, а також періодичну перевірку сумісності та продуктивності системи для підтримання стабільності продукту в майбутньому.

Отже, оновлений план тестування зберігає основну структуру без істотних змін і надалі орієнтований на підтримку стабільності та контроль якості після можливих подальших оновлень.

---

- [x] *Савеля Максим*
- [ ] *Зорій Ярослав*

---
[:arrow_up: Повернутись до початку етапу](/docs/4.Stabilizing/README.md)
